* 创建正则表达式
     * 字面量方式 - 推荐
       var 正则表达式 = /规则/修饰符
       * 规则 - 字母、数字或符号等
       * 修饰符 - 匹配的规则
     * 创建对象方式
       var 正则表达式 = new RegExp(规则, 修饰符);

 * 正则表达式的规则
     /^[字符]{n,m}$/
     * ^ - 表示以什么开始
     * $ - 表示以什么结束
     * [] - 匹配任意字符
     * {n,m} - 表示出现至少 n 次，最多 m 次

* String类型
   * 正则表达式匹配的方法
     * match() - 匹配
       * searchvalue - 匹配的字符串
       * regexp - 匹配的正则表达式
     * replace(regexp,replacement) - 替换
     * search() - 搜索
   * length属性
     * 作用 - 返回当前的字符串的长度(字符串包含字符的个数)
   * 常见操作
     * 获取指定位置的字符 - charAt()
     *